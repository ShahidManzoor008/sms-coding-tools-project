const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://localhost:5000";

// ✅ Debugging: Log the API URL to ensure it's loaded
// console.log("🚀 API_BASE_URL:", API_BASE_URL);

export default API_BASE_URL;
